import styles from './Components.module.sass';
import {FC} from "react";

const Footer: FC = () => {
    return (
        <footer className={styles.footer}>Created by AKA</footer>
    );
};

export default Footer;
